<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || "Contact Us - CKC" %></title>
    <link href="/stylesheets/output.css" rel="stylesheet">
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
</head>
<body class="bg-light text-gray-900 overflow-x-hidden">

<!-- Navbar -->
<%- include('partials/nav') %>

<%- include('partials/hero') %>

<!-- Contact Section -->
<section class="py-16 bg-white">
    <div class="container max-w-6xl mx-auto px-6">
        <h2 class="text-4xl font-bold text-primary text-center">Get in Touch</h2>
        <p class="text-gray-600 mt-4 text-lg text-center">We'd love to hear from you! Let’s discuss how CKC Consulting can help with your next project.</p>

        <!-- Contact Card -->
        <div class="mt-10 flex justify-center">
            <div class="p-8 bg-light rounded-xl shadow-md text-center w-full max-w-lg border border-gray-200">
                <h3 class="text-2xl font-semibold text-secondary">📧 Email Us</h3>
                <p class="mt-3 text-gray-700 text-lg font-medium">
                    <a href="mailto:<%= email %>" class="text-primary font-bold hover:underline">
                        <%= email %>
                    </a>
                </p>
                <p class="mt-2 text-gray-600 text-sm">
                    We typically respond within 24 hours.
                </p>
            </div>
        </div>
    </div>
</section>

<!-- Contact Form -->
<section class="relative py-20 bg-gray-100">
    <!-- Subtle Background Shape -->
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_top_left,_rgba(245, 245, 245, 0.7)_0%,_transparent_60%)]"></div>

    <div class="container max-w-4xl mx-auto px-6 relative z-10">
        <h2 class="text-4xl font-bold text-primary text-center">Send Us a Message</h2>
        <p class="text-gray-700 mt-4 text-lg text-center">We’ll get back to you as soon as possible.</p>

        <form action="/send-message" method="POST" class="mt-10 bg-white p-8 rounded-lg shadow-lg border border-gray-200">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <!-- Name -->
                <div>
                    <label for="name" class="block text-gray-700 font-semibold">Full Name</label>
                    <input type="text" id="name" name="name" required class="w-full mt-2 p-3 border border-gray-300 rounded-lg focus:ring focus:ring-primary">
                </div>

                <!-- Email -->
                <div>
                    <label for="email" class="block text-gray-700 font-semibold">Email Address</label>
                    <input type="email" id="email" name="email" required class="w-full mt-2 p-3 border border-gray-300 rounded-lg focus:ring focus:ring-primary">
                </div>
            </div>

            <!-- Message -->
            <div class="mt-6">
                <label for="message" class="block text-gray-700 font-semibold">Your Message</label>
                <textarea id="message" name="message" rows="5" required class="w-full mt-2 p-3 border border-gray-300 rounded-lg focus:ring focus:ring-primary"></textarea>
            </div>

            <!-- Submit Button -->
            <button type="submit" class="w-full bg-primary text-white py-3 rounded-lg font-bold hover:bg-secondary transition mt-6">
                Send Message
            </button>
        </form>
    </div>
</section>

<!-- Call to Action -->
<%- include('partials/call_to_action') %>

<!-- Footer -->
<%- include('partials/footer') %>

</body>
</html>
