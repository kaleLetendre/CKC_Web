<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || "Contact Us - CKC" %></title>
    <link href="/stylesheets/output.css" rel="stylesheet">
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
</head>
<body class="bg-light text-gray-900 overflow-x-hidden">

<!-- Navbar -->
<%- include('partials/nav') %>

<%- include('partials/hero') %>

    <!-- Contact Information (Email Only) -->
    <section class="py-20 bg-white">
        <div class="container max-w-6xl mx-auto px-6 text-center">
            <h2 class="text-4xl font-bold text-primary">Get in Touch</h2>
            <p class="text-gray-700 mt-6 text-lg leading-relaxed">
                Reach out to us for a free consultation. Whether it’s a simple website or a complex platform, we’d love to help!
            </p>

            <div class="mt-10 flex justify-center">
                <div class="p-8 bg-gray-100 rounded-xl shadow-md text-center">
                    <h3 class="text-2xl font-semibold text-secondary">📧 Email</h3>
                    <p class="mt-2 text-gray-700"><%=email%></p>
                </div>
            </div>
        </div>
    </section>

    <!-- Contact Form -->
    <section class="py-20 bg-gray-100">
        <div class="container max-w-4xl mx-auto px-6">
            <h2 class="text-4xl font-bold text-primary text-center">Send Us a Message</h2>
            <p class="text-gray-700 mt-4 text-lg text-center">We’ll get back to you as soon as possible.</p>

            <form action="/send-message" method="POST" class="mt-10 bg-white p-8 rounded-lg shadow-lg">
                <div class="mb-6">
                    <label for="name" class="block text-gray-700 font-semibold">Full Name</label>
                    <input type="text" id="name" name="name" required class="w-full mt-2 p-3 border border-gray-300 rounded-lg focus:ring focus:ring-primary">
                </div>

                <div class="mb-6">
                    <label for="email" class="block text-gray-700 font-semibold">Email Address</label>
                    <input type="email" id="email" name="email" required class="w-full mt-2 p-3 border border-gray-300 rounded-lg focus:ring focus:ring-primary">
                </div>

                <div class="mb-6">
                    <label for="message" class="block text-gray-700 font-semibold">Your Message</label>
                    <textarea id="message" name="message" rows="5" required class="w-full mt-2 p-3 border border-gray-300 rounded-lg focus:ring focus:ring-primary"></textarea>
                </div>

                <button type="submit" class="w-full bg-primary text-white py-3 rounded-lg font-bold hover:bg-secondary transition">
                    Send Message
                </button>
            </form>
        </div>
    </section>

    <!-- Call to Action -->
    <%- include('partials/call_to_action') %>

    <!-- Footer -->
    <%- include('partials/footer') %>

</body>
</html>
